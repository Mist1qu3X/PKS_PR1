<Window x:Class="LibraryApp.GenresWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление жанрами" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Таблица жанров -->
        <DataGrid x:Name="GenresGrid" Grid.Row="0" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="250"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Добавить" Width="80" Margin="5" Padding="5" Click="AddGenre_Click"/>
            <Button Content="Изменить" Width="80" Margin="5" Padding="5" Click="EditGenre_Click"/>
            <Button Content="Удалить" Width="80" Margin="5" Padding="5" Click="DeleteGenre_Click"/>
        </StackPanel>
    </Grid>
</Window>